#include<bits/stdc++.h>
#include<graphics.h>
#include<conio.h>
using namespace std;

int main() {
    int gd = DETECT, gm;
    initgraph(&gd, &gm, "C:\\tc\\bgi");

    setcolor(GREEN);
    rectangle(100, 100, 300, 220);
    for(int i = 100; i <= ){

    }
    setcolor(RED);
    circle(200, 160, 30);


    getch();
    closegraph();
}

