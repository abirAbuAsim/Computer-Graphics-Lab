#include<bits/stdc++.h>
#include<graphics.h>
#include<conio.h>
using namespace std;

int main() {
    int gd = DETECT, gm;
    initgraph(&gd, &gm, "C:\\tc\\bgi");

    //outtextxy(110, 60, "A");

    line(10, 10, 50, 50);
    getch();
    closegraph();
}
